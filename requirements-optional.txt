# Scene Ripper - Optional dependencies (downloaded on demand)
# These are NOT bundled in the .app and are installed when features are first used.

# Video download
yt-dlp>=2024.1

# ML / Deep Learning (local inference)
# torch and torchvision MUST be installed together to stay version-compatible
torch>=2.4,<2.7
transformers>=4.50,<5
torchvision>=0.19,<0.22
einops>=0.7.0
sentencepiece>=0.2.0
protobuf>=4.25,<6

# Apple Silicon acceleration (mlx stack — macOS ARM only)
mlx-vlm>=0.1.0; sys_platform == 'darwin' and platform_machine == 'arm64'
lightning-whisper-mlx>=0.0.10; sys_platform == 'darwin' and platform_machine == 'arm64'

# Transcription
faster-whisper>=1.0.0

# Object detection
ultralytics>=8.4.0

# OCR / Text Extraction
paddleocr>=3.0.0
rapidfuzz>=3.0.0

# Audio Analysis
librosa>=0.10.0

# Stem Separation
demucs-infer>=4.1.0
