<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow unsigned executable memory (required for Python/NumPy) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Disable library validation (required for loading on-demand .so/.dylib) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Allow JIT compilation (required for NumPy/PyTorch JIT) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- Network client access (for downloads and API calls) -->
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- Read-write access to user-selected files (for video file access) -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
</dict>
</plist>
